- type: custom:simple-weather-card                    
  style: |
    ha-card {
      border-radius: var(--border-radius);
      box-shadow: var(--box-shadow);
      opacity: 0.8;
      font-size: var(--name-font-size);
      font-family: var(--font-family);
    }                      
  entity: weather.nafels
  name: Näfels
  backdrop: false
  tap_action:
    action: navigate
    navigation_path: /phone-view/weather